name: Test s390x Runner

on: [push]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check architecture
        run: uname -m

      - name: Check OS
        run: cat /etc/os-release
